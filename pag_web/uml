    - reqs
      - nombre
      - esencial
    - presupuestos
      - nombre
      - clave (obra/cliente/tipo_presu+consecutivo)
      //- requisitos (array con json {nombre, entregado, esencial, timestamps:{startedAt, entrega}})
      - horas_programadas
      - cash_presupuestado
      - timestamps: {startedAt, finishedAt, activacion, reqs_completados}
      - contrato
      - reqs // Se esta jalando de un array, hay que subirlo con el json de arriba
      - exclusiones
      - pagos (luego vemos si es el total pagado o los pagos)
      - consecutivos: consecutivo_num->{precio, pdf_url, checkin}//checar que si se llama asi
      - oculto
    - registros: (con push asi que con clave unica) 
        - cu
        - inge
        - obra 
        - presupuesto
        - checkin 
        - horas
        - status
    - obra (por nombre)
      - nombre
      - clave (max 6 carac)
      - direccion: {calle, numero, colonia, delegacion, ciudad, cp}
      //- lider
      - cliente: nombre 
      //- asignados (array con json {nombre})
      - timestamps: {startedAt, finishedAt}
      - presupuestos: nombre -> {todo lo de arriba}
    - inges (por uid)
      - uid
      - nombre
      - email
      - especialidad
      //- proyecto_asignado
      - credenciales
      - status
      - obras: nombre-> nombre_presupuesto -> { presupuesto, horas_trabajadas} //, horas_invalidas}
      - permisos: (nombre de las paginas, una por una, y el valor es boolean) alta_colaborador, alta_obra, alta_cliente, reoprte, perfil
    - clientes (por nombre)
        - nombre
        - clave
        - telefono (de la empresa)
        - atencion: nombre -> {nombre, area, celular, email, extension}
        - direccion: {calle, numero, colonia, delegacion, ciudad, cp}
    - tipos_presupuesto (por nombre)
        - nombre
        - codigo
    - exclusiones (por nombre)
        - nombre 
        - texto
